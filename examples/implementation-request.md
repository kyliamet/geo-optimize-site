# Audit and implementation request

```text
$geo-optimize-site audit this site and implement the highest-value GEO improvements.

Preserve the current visual design and conversion flow. Use only facts supported by the repository and the source material I supplied. Verify current crawler behavior using official provider documentation before changing robots.txt. Run the existing checks and inspect the changed pages locally.

Do not commit, push, deploy, or create a pull request.
```

Expected behavior:

- The skill identifies the framework, routes, rendering model, and existing SEO/GEO setup before editing.
- Changes are proportional to evidence found on the site.
- Structured data remains consistent with visible content.
- The result includes concrete validation and unresolved factual questions.
- Local implementation does not imply permission to publish changes.
